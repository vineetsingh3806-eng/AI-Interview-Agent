import type { InterviewSession, Candidate } from "@/types";
export function buildFeedbackPrompt(session:InterviewSession,candidate:Candidate){
 const history=session.questionHistory.map(q=>({question:q.question,topic:q.topic,day:q.day,score:q.score??0,skipped:q.skipped,evaluation:q.evaluation}));
 return `You are a senior hiring panelist. Produce an evidence-based final interview report for ${session.candidateName||candidate.name}. Do not invent achievements. Use only the interview evidence. Candidate profile: ${candidate.bio}. Completed days: ${candidate.completedDays.join(",")}. Weak topics: ${candidate.weakTopics.join(",")}. Strong topics: ${candidate.strongTopics.join(",")}. Interview evidence: ${JSON.stringify(history)}.\nReturn ONLY JSON with: overallScore 0-100, topicScores [{topic,day,score,questionsAsked,questionsAnswered,coverage}], dimensions [{name,score,description}] for Technical Accuracy, Communication, Problem Solving, System Thinking, Practical Knowledge; strengths[], weaknesses[], missedConcepts[], improvementPlan[], recommendedDays[], recommendedProjects[], hiringRecommendation one of strong-hire|hire|borderline|no-hire, hiringRationale, executiveSummary.`;
}
